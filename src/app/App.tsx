import { SafeAreaView, StyleSheet, Text, View } from 'react-native';
import "../../global.css"; // Make sure this is required and set up correctly for Expo



export default function App() {
  

  return (
    <SafeAreaView >
      <View style={{ height: '100%', width: 40, backgroundColor: 'slategray' }}>
        <Text>hello</Text>
      </View>
    </SafeAreaView>
  );
}
